export const profile = {
  userId: String,              
  firstName: String,
  lastName: String,
  birthDate: Date || undefined,
  location: String || undefined,
  language: String || 'en',
  avatarUrl: String || undefined,
  bio: String,
}